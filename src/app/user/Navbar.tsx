"use client";

const Navbar = () => {
  return (
    <div
      className={`bg-[#324B51] max-w-screen overflow-auto md:w-full h-20 text-white font-semibold text-xl flex items-center transition-all`}
    >
      <div className="pl-10">User Account Dashboard</div>
    </div>
  );
};

export default Navbar;
