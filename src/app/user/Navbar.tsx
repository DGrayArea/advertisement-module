"use client";

const Navbar = () => {
  return (
    <div
      className={`bg-[#324B51] w-full h-20 text-white font-semibold text-xl flex items-center transition-all`}
    >
      <div className="pl-10">User Account Dashboard</div>
    </div>
  );
};

export default Navbar;
