"use client";

import SearchNFilters from "../SearchNFilters";

const Users = () => {
  return (
    <div
      className={`bg-[#E9ECF0] max-w-screen overflow-auto md:w-full h-full p-6`}
    >
      <SearchNFilters />
    </div>
  );
};

export default Users;
